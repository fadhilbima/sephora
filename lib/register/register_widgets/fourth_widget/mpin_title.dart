import 'package:flutter/material.dart';

class MpinTitle extends StatelessWidget {
  const MpinTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Buat MPIN', style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600));
  }
}
