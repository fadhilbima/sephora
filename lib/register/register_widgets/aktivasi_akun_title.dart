import 'package:flutter/material.dart';

class AktivasiTitle extends StatelessWidget {
  const AktivasiTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Aktivasi Akun', style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600));
  }
}
