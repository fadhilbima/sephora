import 'package:flutter/material.dart';

class PermissionTitle extends StatelessWidget {
  const PermissionTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Syarat & Ketentuan', style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600));
  }
}
